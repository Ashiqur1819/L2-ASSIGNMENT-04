enum Role {
    CUSTOMER
    TECHNICIAN
    ADMIN
}

enum Status {
    ACTIVE
    BLOCKED
}

enum BookingStatus {
    REQUESTED
    ACCEPTED
    DECLINED
    PAID
    IN_PROGRESS
    COMPLETED
    CANCELLED
}

enum Day {
    MONDAY
    TUESDAY
    WEDNESDAY
    THURSDAY
    FRIDAY
    SATURDAY
    SUNDAY
}

enum PaymentMethod {
    CARD
}

enum PaymentProvider {
    STRIPE
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
